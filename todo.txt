- VerticalPhotoList
    - Add instructions at the top
    - Add a done button at the top
    - Callback with results from position_pickers

- PanMotorToFovConsistencyCheckers
    - Visualize FOV
    - Choose regions with fixed FOV and large pan
    - Show an image from extremes
    - Ask the user to pick the same object in each image
    - Calculate angle
    - Compare to pan

x TagGpsTimebaseAligner
    x Handle load video button
    - Done button saves data to yml
    - yml format
        {
            aggregate_results:
                {
                    alignment_stats:
                        {
                            delay_tag_gps_to_video:
                                {
                                    max:
                                    mean:
                                    min:
                                    num:
                                }
                        }
                }

            individual_results:
                [
                    {
                        delay_tag_gps_to_video:
                        tag_data:
                            {
                                tag_idx:
                                tag_time:
                            }
                        video_data:
                            {
                                frame_num:
                                time_ms:
                                video_id:
                                video_url:
                            }
                    }

                ]


- GeoMapScrubber
    - scrolls in such a way as to keep the dot in the middle
    - SliderMouseHandler
        - Freeze
    x Slider
        x 'p' keystroke to jump to Selected