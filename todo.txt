- VerticalPhotoList
    - Add instructions at the top
    - Add a done button at the top
    - Callback with results from position_pickers


- PanMotorToFovConsistencyChecker
    - Visualize FOV
    - Choose regions with fixed FOV and large pan
    - Show an image from extremes
    - Ask the user to pick the same object in each image
    - Calculate angle
    - Compare to pan

- TagGpsTimebaseAligner
  - Show plan view with map and scrubber
    - Get extents of lat long data
    - Get map of area with extents

  - Show video with scrubber
  - Scrub to same point on map and video

- GeoMapper
    x Save image in a directory with params as the filename
    x Check if image exists before making the request
    x Get gps location from tag data
    - At the tightest zoom figure out

    - For zoom
        - 20
        - 21
    - Figure deg lat per pixel
    - Calculate deg long per pixel based on latitude
    - For a set of points
        - Get the corners
        - Add 10 foot border
        - Get the center
        - Determine zoom for best fit given 640x640 is max size
        - Determine the dimensions to get best fit

